using System;

namespace Practice
{
    class Program
    {
        static void Main(string[] args)
        {
            
            // for(int i = 0;i < intArray.Length-1;i++)
            // {
            //     if(intArray[i] < intArray[i+1])
            //     {
            //         index++;
            //         if(maxIndex <= index)
            //         {
            //             maxIndex = index;
            //         }

            //     }
            //     if(intArray[i]>=intArray[i+1])
            //     {
            //         index = 1;
            //     }
            // }
            // Console.Write(maxIndex);

            // Array.Sort(intArray);
            // Console.Write(intArray[intArray.Length-2]);
        }

        static void Hui()
        {
            //回文串
            // string str = Console.ReadLine();
            // bool isHui = true;
            // for(int i = 0;i < str.Length/2;i++)
            // {
            //     if(str[i]!=str[str.Length -1 -i])
            //     {
            //         isHui = false;
            //         break;
            //     }
            // }
            // if(isHui)
            // {
            //     Console.WriteLine("是回文串");
            // }
            // else
            // {
            //     Console.WriteLine("不是回文串");
            // }
        }
    }
}
